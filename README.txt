Установить Python 3.x

В командной строке:

python3 install -r requirements.txt
python3 exec.py output <Путь для json-файла>

python3 exec.py -h для помощи

json файл в формате:

{
ОВД:{
  нарушение:float
    }
}

В папке data в файле ovd_offence_report можно посмотреть, что получилось
